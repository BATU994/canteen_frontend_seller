// part of 'dio_rest_client.dart';

// class DioConfigurations {
//   static const Duration connectTimeout = Duration(milliseconds: 90000);
//   static const Duration receiveTimeout = Duration(milliseconds: 90000);
//   static const Duration sendTimeout = Duration(milliseconds: 90000);
// }
